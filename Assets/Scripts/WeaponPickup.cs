using UnityEngine;
using System.Collections;

public class WeaponPickup : MonoBehaviour {

    public GunData type;
    void OnTriggerEnter(Collider c)
    {
        //if(c.GetComponentInChildren<GunData>().current
    }
}
